Чтобы назначить текстуру в материал, достаточно в разделе Texture включить необходимое изображение в Image;
Не забываем так же, что текстура хранится всегда рядом с .dae файлом, и текстуру необходимо копировать вместе с .dae, а иначе её неоткуда будет загрузить.
